# AGENTS.md | 全域 AI Agent 核心架構與工程中樞協定

> **專案作者**: 許哲誠 (HSU, CHE-CHENG)[cite: 3]  
> **協定定位**: AI 與工程師協作之最高工程中樞。負責仲裁順位、指令防禦、代碼品質底線與設計文檔規範。[cite: 3]

---

## 0. 核心防禦與批次確認機制 (Confirmation & Execution Guardrails)

本章節確立命令執行邊界與確認機制，兼顧資安防呆與極致流暢度。

### 0.1 指令確認分級
* **極速直接執行 (免確認)**: 所有非破壞性分支切換、狀態檢查、日常進度拉取與沙盒暫存（如 `push到feature`）。
* **單行批次確認 (發布指令)**: 凡執行 `push到dev` 或 `push到master`，AI 僅以單行確認：
  > 「即將執行發布『[指令名稱]』。請確認：1. 遠端推送 [Y/N] 2. 文檔、README 與 CHANGELOG 更新 [Y/N]（回覆 YY / YN / N）」
  * 回覆 `YY`：執行推送並同步更新文檔庫與 `CHANGELOG.md`。
  * 回覆 `YN`：極速推送，略過文檔重構與歷程沉澱。
  * 回覆 `N` 或其他歧義輸入：立即終止並待命。
* **高危操作阻斷 (破壞性指令)**: `git 重置化` 與 `倒回上一個 commit` 強制單獨確認影響範圍後始得執行。

### 0.2 優先級仲裁順序
1. **歧義主動提問**: 缺乏核心參數或架構選型依據時，主動提問確認。[cite: 3]
2. **資安與零幻覺原則**: 嚴格落實金鑰隔離與開源授權；查無實據之 API 或套件禁止捏造。[cite: 3]
3. **特化協定原生條款**: 特化協定中的語言原生語法與檔名規則，強制覆蓋通用規範。[cite: 3]
4. **修訂邊界隔離**: 明確指定修改範圍時，嚴禁變動無關程式碼。[cite: 3]
5. **純分析只讀防禦**: 指令包含『分析』或要求審查時，維持只讀狀態，嚴禁修改或建立任何檔案。[cite: 3]
6. **人類明確覆寫**: 使用者明確指示覆寫某規則時始得例外處理。[cite: 3]

---

## 1. 語言、代碼註解與文檔規範 (Communication & Documentation)

本章節定義文字、語系與排版底線。

### 1.1 語言與註解規範
* **對話語言**: 強制使用臺灣繁體中文（「臺」一律使用「臺」，嚴禁「台」與簡體字）。[cite: 3]
* **術語標準**: 保留標準英文（如 Async/Await, Shader, Middleware, Controller, DTO, Repository, DI, Mermaid）。[cite: 3]
* **程式碼註解**:
  * 核心模組或全新架構檔案頂部具備繁體中文 Header 區塊註解（說明職責、依賴與邊界）。[cite: 3]
  * 內部代碼註解一律使用頂級英文，專注解釋架構意圖（"Why"）而非字面操作。[cite: 3]

### 1.2 排版與文檔色彩規範
* **導言規範**: 每個 Markdown 標題（`#`, `##`, `###`）下方必須具備一段導言說明，嚴禁標題後直接放置小標題、清單或代碼區塊。[cite: 3]
* **重點色彩標註**: 發布正式文檔（`README.md`、`CHANGELOG.md` 與系統規格書）時，核心元件採用反引號、關鍵指標加粗標註、安全禁令以高警示色彩突出。[cite: 3]日常微調任務豁免微觀樣式稽核。
* **Mermaid 統一預設風格**: 圖表採用沉穩暗色科技風（背景 `#030712`、卡片 `#0b0f19`、邊框高亮 `#00f0ff`、文字 `#f8fafc`），連線配置 `curve: 'linear'`，杜絕 inline style 雜色。[cite: 3]

---

## 2. 軟體工程底線：SOLID 與 Clean Code (Engineering Standards)

本章節定義所有程式碼產出之架構標準。

* **SRP 單一職責**: DTO、Repository、Service 與 Controller 強制物理分層隔離。[cite: 3]
* **OCP 開放封閉**: 商業邏輯擴充優先採用策略模式或多型，避免龐大 switch-case 分支。[cite: 3]
* **LSP 里氏替換**: 子類別必須保證完全相容父類別契約，禁止拋出 `NotSupportedException` 或實作空方法。[cite: 3]
* **ISP 介面隔離**: 拆分為細粒度角色介面，避免肥大介面。[cite: 3]
* **DIP 依賴反轉**: 高階模組依賴抽象介面，全面導入依賴注入 (DI)。[cite: 3]
* **防禦性程式設計**: 函式專注單一抽象層級；強制早期返回 (Early Return)；嚴禁靜默吞掉例外。[cite: 3]

---

## 3. 專案文檔拓撲與發布沉澱體系

本章節規範跨專案結構一致性與唯一真實來源（SSOT）。

### 3.1 根目錄 README 六大核心結構
根目錄 `README.md` 建立與更新時，必須具備以下完整架構：
1. **專案願景與核心價值 (Project Overview)**: 業務目標、系統亮點與作者資訊。[cite: 2]
2. **系統架構與工程模型 (Architecture Design)**: 包含 C4 Model、UML 類別圖與即時資料流循序圖。[cite: 2]
3. **技術棧選型矩陣 (Technology Stack)**: 視圖層、狀態流、持久化與部署基礎設施。[cite: 2]
4. **目錄結構拓撲 (Directory Topology)**: 清晰之實體原始碼樹狀層級。[cite: 2]
5. **本地開發與建置指引 (Build & Run Setup)**: 套件安裝、啟動與構建步驟。[cite: 2]
6. **工程品質指標 (Engineering Metrics)**: 0 型別錯誤、構建效能與測試覆蓋驗收。[cite: 2]

### 3.2 系統分析規格清單 (`docs/system-design/`)
各專案依具備範疇按需建立，缺省模組不影響其他檔案：
* `01-overview.md`: 系統願景、邊界、C4 Model 容器架構
* `02-tech-stack.md`: 技術選型矩陣、相依庫與版本理由
* `03-architecture.md`: 代碼庫結構、DDD 分層與模組依賴拓撲
* `04-specs-frontend.md`: 前端展示模組、UI 狀態管理與防禦（純後端省略）
* `04-specs-backend.md`: 後端 API 合約、領域邏輯與交易邊界（純前端省略）
* `04-specs-gameplay.md`: 核心遊戲迴圈、狀態機與數值規格（非遊戲專案省略）
* `05-specs-database.md`: 資料庫 ERD、快取策略與正規化結構（無 DB 省略）
* `06-flowcharts.md`: 業務流程圖、操作流與狀態轉移
* `07-uml-diagrams.md`: Mermaid 類別圖 (Class) 與循序圖 (Sequence)
* `08-ui-ux-standards.md`: 設計系統規範與 WCAG 無障礙（純後端/CLI 省略）
* `09-devops-deployment.md`: 容器構建、CI/CD 與運維監控（輕量專案省略）

### 3.3 變更歷史與日常交付
* **根目錄 `CHANGELOG.md`**: 唯一全域修訂歷程。日常開發不碰，僅在發布確認時於頂端追加階段成果。
* **日常微調交付**: 日常對話結束時以 2 至 3 行簡要回報驗收成果，直接單次提交結案。

---

## 4. Mermaid 防崩潰硬性指標 (Mermaid Zero-Crash Protocol)

* **字元包裹**: 連線標籤含有括號、斜線等符號時，強制 100% 以雙引號包裹（`-->|"文字"|`）。[cite: 3]
* **Stereotype 規範**: 類別圖僅允許標準單詞 ASCII stereotype（如 `<<interface>>`），嚴禁包含中文字元或空格。[cite: 3]
* **流程圖格式**: 決策節點統一採用標準矩形卡片格式 `id["決策描述"]`，禁止使用易造成詞法解析錯誤的菱形語法。[cite: 3]
* **保留字元防護**: 節點文字嚴禁出現裸露之 `&`，改用繁體中文「與」或英文「and」。[cite: 3]
* **單向俐落流動**: 連線採單向階層流動（TD 或 LR），避免跨層反向繞圈線條。[cite: 3]

---

## 5. 核心交付指令 SOP (Core Commands)

* **`專案最佳化`** (非 Unity 專案專用，⚠️ Unity 專案嚴格禁止執行)[cite: 3]:
  1. 檢驗套件管理器（Web 專案優先 pnpm），確認無幽靈依賴。[cite: 3]
  2. 代碼庫淨化：移除 Dead Code、無用除錯日誌與空白目錄。[cite: 3]
  3. 品質審查：核心模組補齊繁中 Header 註解；私有成員落實 `private _camelCase`。[cite: 3]
  4. 構建驗證：執行 Build 指令確保 0 型別錯誤。
  5. 結轉回報：完成沙盒提交。
* **`專案系統分析建立` / `專案系統分析更新`**:
  依真實原始碼逆向稽核，全量建立或精準更新 `docs/system-design/`、`README.md` 與 `CHANGELOG.md`。[cite: 3]