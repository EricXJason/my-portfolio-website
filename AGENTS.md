# AGENTS.md | 全域 AI Agent 最高工業級核心工程協定

> **專案作者**: 許哲誠 (HSU, CHE-CHENG)  
> **標準規範**: AI 與人類協作之最高工業級生產標準。本文件為所有 AI Agent 之一條龍全域核心中樞協定。負責系統優先級裁決、全域指令前置確認防禦、溝通互動約束、軟體工程底線 (SOLID / Clean Code)、頂級系統分析師文檔工程架構 (`docs/system-design/`) 以及特化模組協定調度。  
> *Release: 2026-09-14*

---

## 核心交付指令速查 (Core Command Reference)

本區塊定義全域工程交付之總指揮指令。版控操作強制移交 `AGENTS-GIT.md`；特化協定（FRONT, BACK, UNITY）為純靜態規範庫，不包含任何指令。**注意：執行指令前強制觸發第 0.2 節人類確認機制**：

* 指令：`專案最佳化` —— 執行非 Unity 專案之一條龍代碼淨化、註解審查、依賴確認與暫存前置準備（**⚠️ 嚴格禁令：Unity 專案嚴禁執行此指令，避免破壞 GUID 與資產管線**，觸發第 7 章）。
* 指令：`專案系統分析建立` —— 專案初次啟動或首次建置時執行。依實際代碼庫深度逆向稽核，自 01~N 初次建立完整系統設計文檔庫 `docs/system-design/` 與核心圖表全集（觸發第 8 章）。
* 指令：`專案系統分析更新` —— 專案迭代後更新。當架構、API、流程或組件有重大變更，且使用者明確下達本指令時，全量校準更新 `docs/system-design/` 與架構圖表（觸發第 8 章）。

> [!IMPORTANT]
> **AI 極致效能與日常零干擾底線 (Zero-Overhead Performance Firewall)**:
> 1. **System Design 凍結防線**: 在使用者**尚未下達『專案系統分析建立』之前，AI 絕對嚴禁擅自生成 `docs/system-design/`**；日常微小修改、Bug 修復或單一功能迭代中，若使用者**沒有說出『專案系統分析更新』，AI 絕對不用動、嚴禁改動 `docs/system-design/`**！
> 2. **README 凍結防線**: **日常開發任務中，AI 絕對禁止頻繁亂動 `README.md`**；`README.md` 只有在執行 Push 發布流程時才進行建立與更新！
> 3. **Push 前置主動提問攔截**: 當使用者下達 `push到dev` 或 `push到master` 時，AI 強制主動詢問是否同步建立/更新系統分析與 README！

---

## 🧭 Agent 階段生命週期與模組掛載地圖 (Lifecycle & Module Router)

為加速 Agent 閱讀並避免注意力稀釋，Agent 介入專案時請依據專案特徵動態掛載模組，並直接跳轉對應章節進行局部深讀：

```
                    [ 專案啟動 / 接收指令 ]
                              │
               【閱讀 AGENTS.md 第 0 章：優先級、路由與確認機制】
                              │
          ┌───────────────────┼───────────────────┐
          ▼                   ▼                   ▼
    [ Web/前端專案 ]    [ 伺服端/後端專案 ]    [ Unity 遊戲專案 ]
    掛載 AGENTS-FRONT    掛載 AGENTS-BACK    掛載 AGENTS-UNITY
    (靜態規範庫無指令)   (靜態規範庫無指令)   (靜態規範庫無指令)
          │                   │                   │
          │                   │            [ 嚴禁執行最佳化 ]
          │                   │                   │
          └───────────────────┼───────────────────┘
                              ▼
                 [ 涉及版控/提交/分支/推送 ]
                      掛載 AGENTS-GIT
                              │
          [ 收到指令：專案最佳化 / 專案系統分析重構 ]
     【強制先執行第 0.2 節二度確認 -> 執行第 7 章 / 第 8 章】
```

---

## 快速導航與規則交叉索引 (Rule Cross-Reference Index)

本索引表供所有執行任務之 AI Agent 於接收指令瞬間進行前置校準。

| 任務情境與檢查項目 | 強制參照章節與核心約束條款 |
| :--- | :--- |
| **協定載入、環境識別、模組路由** | 強制參照 **第 0.0 節**：專案啟動環境嗅探與特化協定掛載矩陣。 |
| **優先級仲裁順序與衝突處置** | 強制參照 **第 0.1 節**：落實停等提問協定與優先級裁決矩陣。 |
| **全指令執行前強制二次確認機制** | 強制參照 **第 0.2 節**：接收任何 AGENTS 指令前，必須向使用者確認始得執行。 |
| **語言文字、術語保留、註解雙軌制** | 強制參照 **第 1 章**：繁體中文（臺灣「臺」）、保留標準英文術語、頂部中文 Header 與內部純英文註解。 |
| **Git 提交、版控工作流、認證與指令** | 強制參照 **第 2 章與 AGENTS-GIT.md**：強制主動讀取根目錄之 `AGENTS-GIT.md` 執行遠端綁定（SSH 優先/容許 HTTPS）、重置化、三層分支策略、原子回滾與發布。 |
| **軟體品質、SOLID 實作、高內聚低耦合** | 強制參照 **第 3 章**：五大原則落地細則、Clean Code 函數約束與防禦性設計。 |
| **通用檔名與特化覆寫原則** | 強制參照 **第 4 章**：通用預設 kebab-case，特化協定具備最高覆寫權。 |
| **文檔追蹤與單次點收機制** | 強制參照 **第 5.1 與 5.2 節**：checklist 每次重寫、log 最新在頂部（Push 強制永久保留，日常最多 50 筆，分支與 Git 標色）。 |
| **系統分析師規範庫 (docs/system-design/)** | 強制參照 **第 5.3 與第 6 章**：Mermaid 類別圖、資料流、流程圖、雙模式相容與尺寸統一全集規範。 |
| **專案交付最佳化一條龍** | 強制參照 **第 7 章**：指令「專案最佳化」總指揮 SOP（Unity 專案強制禁止使用）。 |
| **系統分析建立與更新一條龍** | 強制參照 **第 8 章**：指令「專案系統分析建立」與「專案系統分析更新」SOP（日常零動、僅在指令下達或 Push 詢問時執行）。 |

---

## 0. 🏆 協定架構、模組路由、優先級裁決與確認機制

本章節確立專案啟動時之協定掛載原則、全域指令安全確認防線，以及規則發生歧異時之裁決順序。

### 0.0 專案啟動環境嗅探與模組路由協定 (Modular Router Protocol)
本條款規範 Agent 介入任何專案時之自動化環境偵測流程：
1. **核心母體**: `AGENTS.md` 永遠常駐生效。
2. **版控防線**: 若專案包含 `.git` 或涉及版控操作，強制載入 `AGENTS-GIT.md`，所有 Git 行為 100% 移交該協定。
3. **特化領域靜態規範庫 (純規範、無指令)**:
   - 檢測到 `Assets/`、`ProjectSettings/`、`.unity` 檔：強制掛載 `AGENTS-UNITY.md`。
   - 檢測到 `package.json`、`vite.config.*`、前端框架目錄：強制掛載 `AGENTS-FRONT.md`。
   - 檢測到 `pom.xml`、`build.gradle`、後端 API 路由或資料庫目錄：強制掛載 `AGENTS-BACK.md`。
4. **全量丟入容錯**: 若使用者將全部協定放入專案根目錄，Agent 維持單一職責原則，僅在處理特定領域時調用對應特化協定，嚴禁跨領域混用規則。

### 0.1 優先級仲裁順位表
本條款定義不可逾越的規則裁決階層：
1. **歧義與未明確需求強制提問**: 當使用者指令缺乏核心參數、技術選型依據不明確或方向模糊時，Agent 嚴禁擅自主張或代為腦補，必須立即暫停並主動向人類提出具體問題釐清方向。
2. **需求衝突自動攔截**: 若新提示詞與 `docs/system-design/` 現存架構規格或核心資安規則衝突，Agent 必須立即暫停執行，主動向人類回報衝突細節，等待指示後始得繼續。
3. **資安、智財合規與零幻覺原則**: 零信任架構、金鑰隔離、開源授權嚴格審查；若所需之套件、API 或資材於合法公開管道查無實據，嚴禁捏造，必須主動回報。
4. **特化協定原生條款 (Domain Specific Overrides)**: 特化協定（`AGENTS-UNITY.md`, `AGENTS-FRONT.md`, `AGENTS-BACK.md`）中的語言原生語法與檔名規範，強制覆蓋通用規範。
5. **嚴格修訂邊界 (Scope Isolation)**: 當指令下達明確修改範圍時（例如「修改按鈕顏色」），絕對禁止改動、重構或變更該範圍以外的任何代碼、文字或樣式。
6. **人類明確覆寫**: 僅當使用者明確指示「忽略某規則」時，才允許暫時覆寫，並須於最終任務回報中註明。
7. **全域 Agent 協定**（本文件規範）。

### 0.2 全域指令前置確認原則 (Command Pre-Execution Confirmation Protocol)
本條款確立所有 Agent 法定指令執行前之絕對防禦防線，杜絕未經授權之自動化執行：
* **強制確認邊界**: 凡接收到 `AGENTS.md` 所定義之指令（如 `專案最佳化`、`專案系統分析建立`、`專案系統分析更新`）或 `AGENTS-GIT.md` 所定義之指令（如 `初始化 git`、`git 重置化`、`倒回上一個 commit`、`切回 dev`、`切回 master`、`pull 最新`、`push到dev`、`push到master`）時，**AI 絕對嚴禁未經確認直接執行任何實質變更**！
* **停等提問格式**: AI 必須立即中斷流程，並使用純繁體中文向使用者提出二次確認：
  > 「檢測到您下達法定指令：『**[指令名稱]**』。
  > 該操作即將執行：[簡述本指令之核心動作與影響範圍]。
  > 請問是否確認立即執行本指令？[Y/N]」
* **確認後放行**: 僅當使用者明確回覆同意（如「Y」、「確認」、「執行」）後，AI 始得進入該指令之標準作業程序（SOP）；若使用者回覆拒絕（如「N」、「取消」），則立即終止該指令並回報待命。

---

## 1. 🧠 互動協定、語言矩陣與排版要求

本章節嚴格規範 Agent 與開發者溝通時之文字風格、排他原則、標題排版及雙軌註解標準。

### 1.1 語言矩陣與專業術語規範
本條款定義對話、代碼與工程文檔之語言表達標準：
* **對話語言**: 於對話介面中，Agent 必須永遠使用**繁體中文 (Traditional Chinese)** 進行回覆。臺灣的「臺」一律使用「臺」，嚴禁使用簡體字、簡寫或「台」。
* **英文術語保留**: 保留業界標準英文術語（例如 Async / Await, Shader, Middleware, Controller, DTO, Repository, Dependency Injection, ScriptableObject, Redux, Hook, Mermaid）。
* **句式禁令**: 嚴禁中英夾雜句式，嚴禁中文後括號補註英文。
* **雙語自然緊鄰排版規範 (先繁中後英文，嚴禁強硬二分法)**:
  * 全域文檔（包含根目錄 `README.md`、`docs/system-design/` 所有文檔與設計規格書）**永遠統一遵循「先繁體中文，再英文」**的自然排版順序。
  * **⚠️ 嚴格禁令：絕對禁止使用 `### 繁體中文` 與 `### English` 等生硬割裂式大標題切分章節！**
  * **⚠️ 嚴格禁令：絕對禁止使用 `- **繁體中文**：...` 與 `- **English**: ...` 等生硬條目標籤！**
  * **標準自然排版風格**:
    * 標題一律採中英並陳：`## 1. 業務痛點與預期承載能力 | Business Pain Points & System Capacity`
    * 正文段落一律先寫完整之繁體中文描述，隨後緊接著英文對照段落（或斜體英文對照 `*English text...*`），呈現流暢專業之國際技術規格書質感。
    * 表格標頭一律採先中文後英文（例如：`技術範疇 (Category)`、`角色職責 (Role & Responsibility)`），儲存格內容亦採先中文後英文對照。
* **程式碼內部雙軌註解規範**:
  * 每個腳本/程式檔**頂部必須具備繁體中文 Header 區塊註解**，詳盡說明該腳本之核心功能、架構職責、依賴關係與生命週期（參照第 3.3 節）。
  * 頂部說明之後的其餘代碼內部註解，強制使用**頂級專業英文**，專注解釋架構意圖（"Why"）而非陳述字面操作（"What"）。

### 1.2 排版與排版格式規範
本條款規範文字產出之結構排版要求：
* **導言強制規範**: 每個標題（`#`, `##`, `###`）下方**必須具備一段導言說明**，嚴禁在標題後直接放置小標題、清單或程式碼區塊。
* **回報精準度**: 任務完成後，精準摘要變更檔案清單、關鍵架構決策與驗證狀態。

### 1.3 開發者專案命名與工程偏好習慣 (Developer Preferences & Habits)
本條款收錄專案作者（許哲誠）之核心工程習慣與視覺偏好，所有 Agent 於專案交付時必須嚴格恪遵：
1. **專案與標題命名去裝飾化 (Clean & Standard Naming)**:
   - 專案標題與文檔名稱嚴禁使用任何裝飾性符號（如 `⚡`、Emoji 等裝飾前綴）。
   - 專案命名採純粹、標準、精準的專業中英文並陳結構（例如：`# My-Portfolio-Website + CMS (個人作品集網站含內容管理系統)`），絕不自創花俏或浮誇詞彙。
2. **圖表視覺美學絕對偏好 (Visual & HUD Aesthetics)**:
   - 圖表偏好沉穩大器的深邃黑夜 Sci-Fi HUD 風格（黑夜背景 `#030712`、極黑灰卡片 `#0b0f19`、賽博青高亮外框與連線 `#00f0ff`、純白字 `#f8fafc`）。
   - 討厭花花綠綠的調色盤雜色與突兀的彩色大色塊。
   - 絕對禁止刺眼的大白底穿透（`subgraph` 容器底色硬體級防護）。
   - 討厭抖動、彎曲、扭曲的多重繞道圓弧線條，連線一律採用筆直俐落的折線（`curve: 'linear'`）。
3. **版面佈局平衡與 1:1 黃金比例 (Visual Balance & Uniform Sizing)**:
   - 嚴格要求同一頁面內的所有圖表（Flowchart、UML Class Diagram、Sequence Diagram、C4 Model）其視覺量體、寬高比、卡片寬度與字體大小必須**1:1 完全等大對稱平衡（約 750px × 360px）**，徹底根除「上一張龐大繞線、下一張窄小萎縮」的失衡現象。
4. **極致防崩潰與雙語標註 (Zero-Crash Syntax & Bilingual Parity)**:
   - 連線標籤含有特殊字元者 100% 雙引號包裹、Stereotype 嚴格單詞 ASCII 化，杜絕任何 Viewer 無法顯示或語法報錯。
   - 全圖表節點、連線說明與文檔章節一律落實中英雙語緊鄰對照。
5. **Git 提交訊息 100% 純英文原則 (Strict 100% Pure English Commits)**:
   - **⚠️ 嚴格禁令**：所有 Git Commit 訊息強制 100% 使用頂級專業英文，**絕對禁止使用任何中文字元**！
   - 嚴格遵循 Conventional Commits 格式：`<type>(<scope>): <imperative English description>`。

### 1.4 全域文檔色彩階層與快速抓重點排版標準 (Speed-Read Visual Hierarchy & Color Standard)
本條款規範全域文檔（包含根目錄 `README.md`、`docs/system-design/` 及 `docs/log.md`）之視覺傳達美學，以「非花俏、重焦點、秒速掌握架構核心」為最高原則：
1. **焦點大於花俏 (Focus Over Flamboyance)**:
   - 嚴禁無意義的彩虹雜色或刺眼裝飾；所有色彩的使用必須具備清晰的「資訊階層（Hierarchy）」與「語意定位（Semantics）」。
2. **一般文字敘述之重點色彩標排標準 (Inline Text Highlighting & Scannability)**:
   - **大段純文字禁止單調黑白灰**: 在一般正文段落、功能清單與架構分析說明時，必須對核心概念進行視覺強化，讓讀者在高速滑動時一眼捕捉關鍵決策。
   - **核心架構與名詞**: 採用反引號 `` `React 19` ``、`` `CmsDirtyContext` `` 或科技色標籤突出。
   - **關鍵量化指標**: 強制反白標註，如 `< 800ms`、`18.7:1 (AAA)`、`0 型別錯誤`。
   - **核心安全與禁令**: 以珊瑚紅或強調字型突出（如 `<font color="#f87171"><b>未存檔阻斷</b></font>`、`⚠️ 嚴格禁令`）。
   - **雙語主次對比**: 中文主文使用高對比純白或深色正文字體，緊隨其後的英文對照使用微透次級斜體 `*English...*`，層次分明、毫不喧賓奪主。
3. **HUD 語意色彩階層規範 (HUD Semantic Color Palette)**:
   - **時間戳記標準色**: Cyber Sky Blue (`#38bdf8`)，用於日期與版本標註。
   - **Git 版控節點標準色**: Royal Purple (`#c084fc`)，用於突出 Commit Hash。
   - **Git 分支識別色**:
     - **`master` 分支**: 珊瑚紅 `<font color="#f87171"><b>master</b></font>`（代表正式生產主線）。
     - **`dev` / `development` 分支**: 琥珀金 `<font color="#fbbf24"><b>dev</b></font>`（代表整合測試基底）。
     - **`feature` 分支**: 天青藍 `<font color="#38bdf8"><b>feature</b></font>`（代表日常開發沙盒）。
   - **變更類型語意色**:
     - `feat`: 翡翠綠 (`#34d399`)
     - `fix`: 珊瑚紅 (`#f87171`)
     - `refactor`: 紫晶 (`#a78bfa`)
     - `docs`: 天青藍 (`#60a5fa`)
     - `style`: 櫻花粉 (`#f472b6`)
     - `chore`: 冷灰 (`#94a3b8`)
     - `perf`: 琥珀金 (`#fbbf24`)
4. **三秒速覽原則 (Three-Second Scan Principle)**:
   - **GitHub 原生 Alerts 視覺錨點**: 於重要架構邊界、不可逾越底線或關鍵設計決策處，適度採用 GitHub Alerts（`> [!IMPORTANT]`, `> [!NOTE]`, `> [!TIP]`），形成高辨識度的彩色導航塊。

---

## 2. 🛡️ 外部協定鏈結與獨立性原則

本章節確立專案模組化協定之調用邊界與獨立運作能力。

* **AGENTS-GIT.md 唯一授權**: 所有涉及 Git 初始化、遠端認證（SSH 優先/HTTPS 容許覆寫）、原子暫存與提交、三層分支拓撲、版本維護（倒回 commit / 切回 dev / 切回 master / pull 最新）、遠端發布推送（push到dev / push到master），以及根目錄 **`README.md` 之全集章節架構、核心架構圖表、雙語對照標準與 Pre-Git-Add 暫存前置審查**，本文件不作重複定義，Agent 必須主動查閱並 100% 遵循根目錄之 `AGENTS-GIT.md`（特別是第 4 章權威規格）。
* **特化規範庫單向查詢**: `AGENTS-FRONT.md`、`AGENTS-BACK.md` 與 `AGENTS-UNITY.md` 僅作為靜態標準規範庫，Agent 依專案類型主動讀取，不得期待其提供額外命令。
* **獨立運作保證**: 本文件不依賴特定特化協定即可常駐運作；若缺乏特化協定，Agent 則遵循一般 Clean Code 與防禦性原則。

---

## 3. 💎 極致工程標準：Clean Code、SOLID 與高內聚低耦合

本章節定義所有程式碼產出之絕對底線，不論任何語言或框架，皆必須貫徹頂級軟體工程原則。

### 3.1 SOLID 原則強制實踐
本條款細化物件導向與模組架構之五大核心原則：
* **單一職責原則 (SRP)**: 一個 Class 或模組僅能因單一理由變更。禁止出現萬能類別（God Object）。資料傳輸（DTO）、資料存取（Repository）、商務決策（Service）與協定轉換（Controller）必須物理隔離。
* **開放封閉原則 (OCP)**: 對擴充開放，對修改封閉。新增商業邏輯時，應透過多型（Polymorphism）、策略模式（Strategy Pattern）或組合（Composition）達成，嚴禁無止境修改既有核心 switch-case 或 if-else 分支。
* **里氏替換原則 (LSP)**: 子類別必須能夠完全替換其父類別而不破壞程式行為與合約。嚴禁子類別拋出 `NotSupportedException` 或覆寫成空函式。
* **介面隔離原則 (ISP)**: 介面應精簡明確，客戶端不應被迫依賴其未使用的方法。強制將龐大胖介面拆分為細粒度角色介面（Role Interfaces）。
* **依賴反轉原則 (DIP)**: 高階模組不應依賴低階模組，兩者皆應依賴抽象；抽象不應依賴細節，細節應依賴抽象。全面導入依賴注入（Dependency Injection, DI）或控制反轉（IoC）。

### 3.2 Clean Code 與防禦性程式設計
本條款規範函數實作與錯誤防護之工程要求：
* **小而精煉的函數**: 單一函數長度原則上不超過 20 行，且僅完成一件事。抽象層級必須單一。
* **早期返回 (Early Return / Guard Clauses)**: 將所有無效參數、邊界失敗或例外狀況於函數前端阻斷，減少巢狀層級。
* **嚴禁靜默吞掉例外 (Swallowed Exceptions)**: 捕獲例外必須記錄結構化 Context 或向上拋出領域專屬例外（Domain Exception）。

### 3.3 腳本頂部繁體中文註解強制規範 (Script Header Documentation)
本條款要求所有程式檔案頂部必須強制包含繁體中文 Header 區塊註解：
```csharp
/**
 * ============================================================================
 * 檔案名稱: OrderPaymentService.cs (或 .ts / .java)
 * 所屬模組: Order System (訂單核心模組)
 * 責任描述: 負責處理交易授權、冪等性防重送校驗與金流狀態更新。
 * 設計模式: 套用 Strategy 模式切換多元金流渠道。
 * 依賴關係: 依賴 IPaymentGateway 與 IOrderRepository。
 * 邊界處理: 處理網路超時重試、重複扣款防禦與併發鎖定。
 * ============================================================================
 */
```

---

## 4. 🔠 檔案系統命名原則與覆寫層級

本章節規範全域檔案系統之通用基底命名風格與各領域覆寫權限。

* **通用預設風格**: 專案一般配置檔、文檔目錄、工具腳本預設強制採用連字號 **`kebab-case`**（例如 `system-config.json`, `setup-env.sh`）。
* **特化覆寫條款**:
  * Unity 專案之 C# 腳本強制遵循 `AGENTS-UNITY.md` 採用 **`PascalCase.cs`**。
  * 後端 Java / C# 類別檔案強制遵循 `AGENTS-BACK.md` 採用 **`PascalCase.java`**。
  * 前端 UI 元件檔案強制遵循 `AGENTS-FRONT.md` 採用 **`PascalCase.tsx`**。

---

## 5. 📜 專案文檔工程矩陣與系統分析師規範 (`docs/`)

本章節確立專案唯一真實來源（Single Source of Truth），規範頂級系統分析師（SA）與軟體架構師之視覺化規格標準，並建立動態追蹤與按需裁剪機制。

### 5.1 專案目錄樹與文檔拓撲結構
本結構圖定義標準工程文檔之目錄佈局。嚴格遵循**零虛構原則（Zero-Hallucination & Reality-First）**與**零跳號嚴格連續順移原則（Zero-Gap Sequential Numbering Protocol）**。任何專案均強制維持內部連續閉環（`01 ~ N`），不缺號、不跳號，同名主題隨專案範疇自然順移遞增：

#### 1. 現代 Web / 前端 / 作品集專案 (純前端標準連續 01 ~ 07 閉環，無資料庫與部署跳號)：
```
docs/
├── checklist.md               # 單次對話點收清單 (每次對話完全覆寫刷新)
├── log.md                     # 全中文修訂歷程 (Push 永久保留，日常上限 50 筆，分支標色)
└── system-design/             # 系統架構設計專案文檔庫 (專案唯一規格真實來源，連續 01~07 閉環)
    ├── 01-overview.md         # 系統目標、邊界、業務價值與 C4 Model 架構拓撲圖
    ├── 02-tech-stack.md       # 技術選型矩陣、框架與相依套件庫規劃 (Web 強制標註 pnpm)
    ├── 03-project-structure.md# 儲存庫實體結構、分層職責與模組依賴關係拓撲圖
    ├── 04-functional-specs.md # 展示模組規格、功能定義、自研 CMS 邊界與狀態防禦
    ├── 05-flowcharts.md       # 核心業務流程圖、使用者操作流與狀態轉移機 (Mermaid)
    ├── 06-uml-diagrams.md     # 系統分析師規格：Mermaid 類別圖 (Class) 與循序圖 (Sequence)
    └── 07-ui-ux-standards.md  # Cyber HUD 戰術美學、跨裝置 RWD 與雙主題 WCAG AAA 標準
```

#### 2. 完整全端系統含 DB 專案 (連續 01 ~ 10 閉環，含實體資料庫、資安與容器部署)：
```
docs/
├── checklist.md               # 單次對話點收清單 (每次對話完全覆寫刷新)
├── log.md                     # 全中文修訂歷程 (Push 永久保留，日常上限 50 筆，分支標色)
└── system-design/             # 系統架構設計專案文檔庫 (專案唯一規格真實來源，連續 01~10 閉環)
    ├── 01-overview.md         # 系統架構目標、分層邊界與 C4 Model 拓撲圖
    ├── 02-tech-stack.md       # 前後端全棧技術選型、微服務框架與中介軟體
    ├── 03-project-structure.md# 全端代碼庫實體結構、模組劃分與依賴關係拓撲圖
    ├── 04-functional-specs.md # 前後端功能模組規格、商業邏輯契約與邊界防禦
    ├── 05-flowcharts.md       # 跨端業務操作流、交易流程圖與分散式狀態轉移機
    ├── 06-data-architecture.md# 資料模型設計、實體關聯圖 (ERD)、快取策略與資料遷移
    ├── 07-uml-diagrams.md     # 系統分析師規格：領域類別圖 (Class) 與跨端循序圖 (Sequence)
    ├── 08-ui-ux-standards.md  # 前端 UI/UX 設計系統、元件庫與無障礙標準
    ├── 09-security-auth.md    # 身分驗證 (JWT/OAuth)、RBAC 權限控管與防禦性資安架構
    └── 10-devops-deployment.md# Docker 容器化、K8s 編排、CI/CD 管線與邊緣部署
```

#### 3. 純單機遊戲專案 (Unity，連續 01 ~ 06 閉環，專注遊戲迴圈與 Gameplay)：
```
docs/
├── checklist.md               # 單次對話點收清單 (每次對話完全覆寫刷新)
├── log.md                     # 全中文修訂歷程 (Push 永久保留，日常上限 50 筆，分支標色)
└── system-design/             # 系統架構設計專案文檔庫 (專案唯一規格真實來源，連續 01~06 閉環)
    ├── 01-overview.md         # 遊戲願景、核心遊戲迴圈 (Core Loop) 與系統架構拓撲
    ├── 02-tech-stack.md       # Unity 引擎版本、URP/HDRP 管線與 Packages 矩陣
    ├── 03-project-structure.md# Assets 目錄拓撲、Assembly Definition 與模組邊界
    ├── 04-gameplay-specs.md   # 核心玩法規格、數值系統、戰鬥邏輯與關卡狀態定義
    ├── 05-flowcharts.md       # 遊戲生命週期流程圖、UI 導航流與狀態機 (FSM)
    └── 06-uml-diagrams.md     # C# 類別關係圖 (Class)、ScriptableObject 架構與事件循序圖
```

#### 4. 純後端 / 微服務專案 (連續 01 ~ 08 閉環，豁免前端 UI/UX)：
```
docs/
├── checklist.md               # 單次對話點收清單 (每次對話完全覆寫刷新)
├── log.md                     # 全中文修訂歷程 (Push 永久保留，日常上限 50 筆，分支標色)
└── system-design/             # 系統架構設計專案文檔庫 (專案唯一規格真實來源，連續 01~08 閉環)
    ├── 01-overview.md         # 微服務架構願景、服務邊界與 C4 Model 系統架構
    ├── 02-tech-stack.md       # 後端語言、框架、資料庫驅動與 Message Broker 選型
    ├── 03-project-structure.md# 服務原始碼拓撲、DDD 分層架構與依賴關係圖
    ├── 04-api-specs.md        # RESTful / gRPC API 介面規格書與錯誤碼對照表
    ├── 05-flowcharts.md       # 核心業務邏輯處理流、非同步事件流與補償狀態機
    ├── 06-data-architecture.md# 資料庫 ERD、正規化結構、Redis 快取與分表分庫設計
    ├── 07-uml-diagrams.md     # 領域模型類別圖 (Domain Class) 與服務間呼叫循序圖
    └── 08-devops-deployment.md# 容器映象構建、Helm Chart、CI/CD 與可觀測性監控 (APM)
```

#### 5. 輕量工具庫 / CLI 專案 (連續 01 ~ 04 閉環，極簡高效架構)：
```
docs/
├── checklist.md               # 單次對話點收清單 (每次對話完全覆寫刷新)
├── log.md                     # 全中文修訂歷程 (Push 永久保留，日常上限 50 筆，分支標色)
└── system-design/             # 系統架構設計專案文檔庫 (專案唯一規格真實來源，連續 01~04 閉環)
    ├── 01-overview.md         # CLI/工具庫定位、命令列參數介面與使用場景
    ├── 02-tech-stack.md       # Runtime 環境、依賴庫審查與打包發布工具選型
    ├── 03-project-structure.md# 源碼目錄結構、公開匯出入口與輔助模組拓撲
    └── 04-api-uml-specs.md    # 核心 API 函式合約、資料型別與 UML 模組類別圖
```

### 5.2 動態追蹤文檔維護矩陣
本矩陣明訂每次對話與任務執行時動態文檔之更新觸發標準：
| 文檔名稱 | 觸發更新條件 | 強制執行動作與格式 | Git 版控狀態 |
| :--- | :--- | :--- | :--- |
| **`docs/system-design/`** | 僅於收到指令『專案系統分析建立』或『專案系統分析更新』，或於 Push 提問獲得同意時 | **專案單一真實來源 (SSOT，日常凍結防線)**：日常微小修改與常規開發絕對不主動更動，僅在指令授權時進行全量校準更新。 | **正常納入 Git**<br>(依 AGENTS-GIT.md 交付) |
| **`docs/checklist.md`** | 每次使用者提出要求並執行任務時 | **單次對話完全覆寫刷新**：每次對話重新生成，記錄本次使用者明確要求與 AI 實際執行動作。多人協作支援請參照 `AGENTS-UNITY.md` 衝突預防機制。 | **正常納入 Git**<br>(依 AGENTS-GIT.md 交付) |
| **`docs/log.md`** | 每次任務完成或準備提交暫存前 | **純中文頂部倒序追加（Push 永久保留，日常上限 50 筆）**：<br>1. **Push 節點永久保留**：凡涉及 `push到dev` / `push到master` 之發布節點強制永久保留。<br>2. **日常紀錄上限 50 筆**：其餘本地原子提交紀錄最多保留最新 50 筆，超出自動淘汰。<br>**標準格式**：`<font color="#38bdf8"><b>[時間]</b></font> \| <font color="{type_color}"><b>[類型]</b></font> \| <font color="#c084fc"><b>git:</b></font> <code>hash</code> [<font color="{branch_color}"><b>分支</b></font>] \| [描述] \| <code>[影響檔案]</code>`。 | **正常納入 Git**<br>(依 AGENTS-GIT.md 交付) |

### 5.3 文檔動態裁剪與零跳號嚴格連續編號原則 (Zero-Gap Continuous Numbering Protocol)
本條款確立防止文檔過度工程、杜絕虛構與保障順暢閱讀體驗之最高底線：

1. **零跳號嚴格連續原則 (Strict Zero-Gap Sequential Flow)**：
   - 任何專案內部的 `docs/system-design/` 文檔，其序號**必須永遠從 `01` 開始嚴格不間斷連續遞增（`01`, `02`, `03`... 直至閉環）**，絕對嚴禁任何缺號、斷號或跳號（例如絕不能出現從 05 直接跳 07 的斷層）。
   - 依序閱讀時，開發者能由淺入深、由巨觀到微觀建立完整系統心智模型，順利無縫接手開發。
2. **專案類型驅動之動態序號順移法則 (Context-Driven Numbering Shift)**：
   - **同名或同主題文檔之編號並非全域固定死板綁定，而是隨專案所屬範疇動態順移遞增**！
   - 例如：
     - **UML 圖表文檔**：在純前端專案中因無實體資料庫，自然緊接在流程圖後編為 **`06-uml-diagrams.md`**；但在全端含 DB 專案中，因 `06` 配置給資料庫 ERD，UML 自然順移編為 **`07-uml-diagrams.md`**；在純單機 Unity 遊戲中，因無 DB 且無 Web 路由，玩法規格為 04、流程為 05，UML 亦自然順移編為 **`06-uml-diagrams.md`**。
     - **UI/UX 規範文檔**：在純前端專案中編為 **`07-ui-ux-standards.md`**；在全端含 DB 專案中順移編為 **`08-ui-ux-standards.md`**；在純後端或 CLI 專案中則完全豁免、不佔號。
3. **專案範疇與連續編號閉環對照矩陣**：
| 專案類型與技術範疇 | 連續必備核心文檔 (Mandatory & Continuous) | 規範說明與邊界約束 (Boundary Enforcement) |
| :--- | :--- | :--- |
| **現代 Web / 前端 / 作品集** | **`01` 至 `07` 全套連續閉環（無跳號）**<br>(01 願景 ➔ 02 技術 ➔ 03 目錄 ➔ 04 功能 ➔ 05 流程 ➔ **06 UML** ➔ **07 UI/UX**) | **嚴格禁止虛構**：無實體後端 API 伺服器與實體資料庫時，嚴禁建立資料庫 (DB)、後端資安 Token、Docker 部署等文檔。 |
| **完整全端系統 (含實體後端與 DB)** | **`01` 至 `10` 全套連續閉環（無跳號）**<br>(01~05, **06 資料庫**, **07 UML**, **08 UI/UX**, 09 後端資安, 10 DevOps) | 僅當專案實體存在後端 API 伺服器與實體資料庫（如 PostgreSQL, MySQL, Redis）時始得建立。 |
| **純單機遊戲 (Unity)** | **`01` 至 `06` 全套連續閉環（無跳號）**<br>(01 願景, 02 引擎, 03 目錄, 04 玩法規格, 05 狀態流程, **06 UML**) | 嚴禁建立 Web 發布、Token 驗證或 Docker 部署文檔。 |
| **純後端 / 微服務** | **`01` 至 `08` 全套連續閉環（無跳號）**<br>(01 願景, 02 技術, 03 目錄, 04 API 規格, 05 業務流程, **06 資料庫**, **07 UML**, 08 部署) | 豁免 UI/UX 規範文檔，文檔無跳號連續至 08。 |
| **輕量工具庫 / CLI** | **`01` 至 `04` 全套連續閉環（無跳號）**<br>(01 願景, 02 技術, 03 目錄, 04 API 與 UML) | 豁免所有 UI、DB、狀態機等文檔，文檔無跳號連續至 04。 |

> **豁免與零虛構絕對禁令**: 任何當前專案未實際具備的架構層次，**絕對禁止建立填寫「無」或空殼/虛構的檔案**，各專案依其真實具備之範疇維持嚴格連續編號（01 ~ N），一律自 01 連續順移、零跳號。

---

## 6. 🛠️ 系統分析師與架構設計全集規格 (`docs/system-design/` 核心細節)

本章節為系統架構設計之全集技術規範標準。文件撰寫必須達到**專業系統分析師 (SA)** 與**頂級架構師**之標準，採用現代通用之 **Mermaid** 語法，確保在 VS Code 與 GitHub 中開箱即用、免額外套件即可原生即時圖形渲染。

### 6.0 文檔資訊密度與後期轉化標準 (High-Density Engineering & Transformation Readiness)
本條款確立文檔必須具備最高規格之工程資訊密度，其內容量體與精確度必須足以在後期直接提取轉化為「系統開發書（架構規格書）」或「技術展示簡報」，無需額外重新調研或人工作業：
1. **深度工程內容完備性 (Deep Engineering Readiness)**：
   - 正文必須具備深入的架構設計脈絡、具體技術選型理由（"Why"）、TypeScript 實體合約程式碼、狀態轉移機真值表與極限邊界防禦條款，確保新進工程師依序閱讀完畢即可 100% 掌握系統並無縫接續開發。
2. **免裝飾與純粹技術質感 (Clean Engineering Documentation)**：
   - 文檔本身為純粹專業之軟體工程技術規格書，**嚴禁在文檔內添加任何「投影片核心摘要卡」、「Slide 標題」或「Slide Tags」等生硬多餘的裝飾性敘述**。
3. **雙語自然緊鄰排版 (先繁中後英文)**：
   - 全文段落與表格一律自然流暢並陳，**永遠先繁體中文描述，隨後緊接英文對照（或斜體英文）**；嚴禁出現獨立孤立的 `### 繁體中文` 或 `### English` 割裂式大標題。

### 6.1 核心視覺化圖表類型規範 (Mermaid Diagram Typology)
本條款規範系統設計文件必須具備的五大核心 Mermaid 圖表規格：
1. **Mermaid 類別關係圖 (依專案連續序號，如純前端 `06-uml-diagrams.md`、全端 `07-uml-diagrams.md` / `classDiagram`)**:
   - 強制採用標準 Mermaid `classDiagram` 語法，確保在 VS Code 與 GitHub 免額外外掛直接原生即時渲染。
   - 清楚標註類別與介面名稱、成員可見性（`+` Public, `-` Private, `#` Protected）、核心方法與參數型別。
   - 明確繪製 Class 間的四大核心關係：繼承（`<|--`）、實作（`<|..`）、組合（`*--`）、聚合（`o--`）與依賴（`..>`）。
2. **模組與檔案依賴拓撲圖 (`03-project-structure.md` / `flowchart`)**:
   - 使用 Mermaid `flowchart` 展示系統檔案間之引用階層與呼叫流向，呈現高內聚低耦合之架構防線。
3. **業務操作流程圖與狀態轉移機 (`05-flowcharts.md` / `flowchart TD`)**:
   - 明確描繪使用者操作流程、身分驗證門禁與未存檔狀態阻斷狀態轉移（State Machine）。
   - **⚠️ 嚴禁使用原生 stateDiagram-v2**：Mermaid 原生 `stateDiagram-v2` 內部寫死高飽和紫色標籤（`#8a2be2`）與淺黃色狀態方塊（`#ffffcc`），無視深色主題並嚴重破版；全專案所有狀態轉移機強制採用標準 `flowchart TD` 實作。
4. **跨層資料交互循序圖 (依專案連續序號，如純前端 `06-uml-diagrams.md`、全端 `07-uml-diagrams.md` / `sequenceDiagram`)**:
   - 使用 Mermaid `sequenceDiagram` 完整展示使用者觸發事件、前端 Component、狀態 Context、自研 CMS 與遠端 API / BaaS 之間的資料同步、狀態轉移與錯誤回滾流程。
5. **C4 容器級架構模型 (`01-overview.md` & `README.md` / C4 Model)**:
   - 呈現角色端（Clients）、應用核心層（Application Core）與基礎設施雲端層（Persistence & Cloud CDN）之階層依賴。

### 6.2 全圖表視覺風格與尺寸量體絕對統一規範 (Unified Visual Language & Sizing)
本條款杜絕圖表各行其是，確立全域圖表一體化工程美學與版面平衡底線：
1. **全圖表設計風格絕對一致**:
   - 無論是 Flowchart、UML 類別圖、循序圖或 C4 架構模型，所有圖表的外觀質感、線條風格、卡片圓角、陰影與文字邊距必須達到 **100% 絕對完全一樣**，禁止出現部分圖表花俏調色盤、部分圖表黑白簡陋之風格割裂。
2. **黃金比例與尺寸量體均衡（嚴禁有大有小）**:
   - 同一文檔或同一頁面（如 `README.md` 第二章架構章節）內的所有圖表，其寬高比與視覺量體必須精準對齊於**協調之黃金比例矩陣（寬度約 700~800px、高度約 350~420px）**。
   - **節點密度均勻化**: 嚴禁一張圖塞入十多個橫向節點與反折大繞線、而下一張圖僅有少數類別縮成窄小一條。圖表節點數量應控制在 6~10 個核心節點，採對稱網格或清晰分層排布，確保字體等大、卡片等大、閱讀節奏穩定舒適。

### 6.3 深淺雙模式高對比度與雙主題可讀性規範 (Dark & Light Dual Theme Compatibility)
本條款建立全環境雙主題無障礙可讀性防護，防止圖表在不同主題預覽器下產生色偏、發虛或刺眼底色：
1. **WCAG 2.2 AA 對比度底線**:
   - 所有圖表文字、線條與背景之間，對比度強制達到 4.5:1 以上（正文）與 3:1 以上（外框與圖形）。
   - **深色模式配置 (Dark Mode)**: 背景統一為深邃黑夜（`#030712`）、卡片底色為深黑灰（`#0b0f19`）、外框與光束線條為賽博青（`#00f0ff`）、文字為高對比純白（`#f8fafc`）。
   - **淺色模式相容性防護 (Light Mode)**: 避免使用過於微弱的淺灰邊框或發虛線條，外框線條粗細至少 1.5px，線條具備充足飽和度（如高對比深天藍 `#0284c7` 或冷墨藍 `#0369a1`），確保在白色或淺灰底預覽器中依然筆直清晰、絕無模糊發虛。
2. **子圖容器底色硬體級防護（嚴禁刺眼白底穿透）**:
   - 在所有使用 `subgraph` 的圖表中，強制於 `themeVariables` 顯式配置 `clusterBkg`（深沉暗夜灰 `#060a14`）、`clusterBorder`（冷科技灰 `#1e293b`）與 `titleColor`（高科技青 `#00f0ff`）。
   - 代碼末端**強制加掛顯式樣式宣告**（如 `style SubgraphId fill:#060a14,stroke:#1e293b,stroke-width:1px,color:#00f0ff`），雙重防禦，絕對杜絕 Markdown 預覽器降級輸出預設刺眼白底。

### 6.4 Mermaid 語法硬指標與防崩潰協定 (Mermaid Zero-Crash Syntax Protocol)
本條款確立全圖表 100% 正常渲染之語法防線，徹底杜絕 Viewer 無法顯示或 Syntax Error：
1. **連線標籤強制雙引號包裹**:
   - 凡連線文字含有半形括號 `()`、斜線 `/`、逗點、頓點、冒號等任何非單詞英數字元者，**強制 100% 使用雙引號包裹**（例如：`-->|"快速測試登入 / 訪客沙盒"|`）。嚴禁裸露括號，徹底根絕 Mermaid lexer 誤判引發之 `Parse error`。
2. **Stereotype 國際標準單詞化**:
   - 在 `classDiagram` 中，嚴格僅允許使用標準單詞 ASCII stereotype（如 `<<interface>>`、`<<abstract>>`、`<<service>>`）。**絕對禁止在 stereotype 內包含中文字元或空格**（如 `<<interface 語言合約>>`），避免觸發 Mermaid 12.0+ 核心語法分析器崩潰。
3. **消除跨層反向繞圈線條**:
   - 圖表連線一律遵循由上而下（TD/TB）或由左而右（LR）之單向階層流動。嚴禁自最底層節點反向跨越全圖外圍大繞圈折回上層，折線一律配置 `curve: 'linear'`，杜絕怪異圓弧與外圍包框線條。
4. **統一採用標準 1:1 矩形 HUD 卡片 (`id["..."]:::hudCard`) 杜絕菱形詞法崩潰**:
   - 菱形判斷節點詞法為 `id{text}`，若寫成 `id{"..."}` 會直接破壞 Mermaid Jison 詞法狀態機，拋出 `Syntax error in text` 炸彈崩潰；若不加引號，標籤內的括號又會拋出 `Parse error`。
   - 全域流程圖之判斷節點一律統一採用 **標準 1:1 矩形 HUD 卡片 `id["..."]:::hudCard`**，並以清晰肯定句描述決策分支（例如 `CheckType["判斷作品是否含 3D 嵌入連結"]`），達成 100% 語法安全與卡片尺寸絕對等大平衡。
5. **嚴禁節點文字包含保留運算子 (`&`)**:
   - Mermaid 流程圖中 `&` 為多節點平行連接保留運算子。節點文字中嚴禁出現裸露之 `&`，一律以繁體中文「與」、「並」或英文「and」取代，防止剖析器誤判。
6. **零腦補真實代碼對齊原則 (Zero-Hallucination Code-First Principle)**:
   - 系統設計圖表必須 100% 依據真實原始碼逆向繪製（例如美術畫廊 3D 檢視以沙盒 `iframe` 嵌入 ArtStation / Sketchfab 播放器與本地 JSON 驅動之 3D 封面輪盤）。嚴禁脫離代碼庫自行腦補不存在的技術框架（如 WebGL 手動記憶體釋放、Three.js OrbitControls、GLB 資產流）。

### 6.5 全圖表中英雙語對照標準 (Bilingual Annotation Standard)
本條款規範圖表之國際化專業交付標準：
* 所有圖表節點文字（Node Label）、狀態描述（State Text）、連線動作（Edge Action）、參與者角色（Actor / Participant）以及各級章節標題與導言，一律強制落實**中英雙語緊鄰對照**（採 `中文名稱 / English Name` 或 `<br>` 換行並陳），確保開源社群、外商審查與技術評審開箱即具備頂級通用性。

---

## 7. 🚀 協定：專案最佳化 (Project Optimization)

本章節規範收到執行指令：「**專案最佳化**」時，Agent 必須強制執行之一條龍完整標準作業程序（SOP）。**注意：執行前必須依第 0.2 節向使用者提出二次確認**。

### ⚠️ Unity 專案絕對禁令 (Strict Ban for Unity)
本條款建立 Unity 專案資產管線保護紅線：
* **檢測到 Unity 專案特徵時（存在 `Assets/`、`ProjectSettings/` 等），Agent 嚴禁執行本章節之最佳化指令！**
* **攔截原因**：Unity 資產依賴 GUID 與 `.meta` 檔案綁定，全端檔案淨化與結構調整極易破壞 Unity 資產引用關係。
* **回報模板**：「⚠️ 警告：當前為 Unity 遊戲專案，本協定嚴格禁止對 Unity 執行『專案最佳化』指令，以防破壞資產管線與 GUID 參照。請依據 `AGENTS-UNITY.md` 規範進行手動審查與維護。」

---

### Web / 全端專案一條龍執行 SOP (僅限非 Unity 專案)

本執行程序規範 Web 與伺服端專案之一條龍標準交付步驟：

1. **套件管理器校驗與切換提問 (Package Manager & pnpm Audit)**:
   - 檢查當前專案是否由 npm 管理（存在 `package-lock.json`）。
   - 若是，**必須立即暫停並主動向使用者提問**：
     > 「檢測到當前專案仍在使用 npm 管理（存在 package-lock.json）。『專案最佳化』標準要求全面升級為 pnpm。請問是否同意切換至 pnpm 並清除舊有 npm 內容？[Y/N]」
   - 若使用者同意（Y），立即執行徹底清除 SOP：刪除 `package-lock.json` 與 `node_modules/`，執行 `pnpm install` 生成 `pnpm-lock.yaml`；若使用者拒絕（N），則維持現狀並於回報中載明。
2. **系統設計文檔檢視**: 讀取並檢視 `docs/system-design/` 相關模組，確保本次改動與系統規格完全吻合。
3. **單次點收與歷程結轉**: 完全刷新 `docs/checklist.md`，並以純繁體中文將本次產出追加至 `docs/log.md` 最上方（依規範維持 Push 永久保留與日常上限 50 筆）。
4. **代碼品質與命名審查**: 確保所有新寫或修改之腳本頂部具備完整的**繁體中文 Header 區塊註解**；私有成員落實 **`private _camelCase`**；若有欠缺後端或其他依賴，落實繁體中文 `TODO:` 規格；審查 Clean Code、SOLID 原則與早期返回。
5. **代碼庫淨化**: 移除無用代碼（Dead Code）、未使用的套件依賴、殘留除錯日誌與空白目錄。
6. **資材合理性檢驗**: 檢查資產檔案引用路徑是否正確，無破損無效連結。
7. **UI/UX 相容性審查**: 審查全裝置直橫向 RWD 相容性與深淺色模式 WCAG 2.2 AA 對比度（若屬免除 UI 之專案類型則跳過）。
8. **前端體驗與 SEO 規範審查**: 涉及 Web/前端專案時，強制委託 `AGENTS-FRONT.md` 執行語意化 HTML、結構化資料標記（JSON-LD）、Meta 標籤矩陣與無障礙相容檢驗。
9. **環境變數與暫存前置準備**: 同步 `.env.example`，確認動態文檔就緒，並移交 `AGENTS-GIT.md` 執行雙語 `README.md` 結構驗證。

---

## 8. 🚀 協定：專案系統分析建立與更新 (System Design Creation & Update)

本章節規範收到執行指令：「**專案系統分析建立**」或「**專案系統分析更新**」時，Agent 必須強制執行之一條龍完整標準作業程序（SOP）。本指令專注於逆向稽核真實代碼庫，全量建立或精準更新 `docs/system-design/` 與 `README.md` 架構圖表全集。**注意：執行前必須依第 0.2 節向使用者提出二次確認**。

### 8.0 核心邊界與效能防禦原則 (Performance & Frequency Firewall)
為保障日常開發之極致流暢與最高執行速度，確立以下絕對防線：
1. **尚未建立防線**：在使用者**尚未明確下達『專案系統分析建立』之前，AI 絕對嚴禁擅自生成 `docs/system-design/`**！
2. **日常零動防線**：日常開發、修改單一檔案或修復 Bug 時，若使用者**沒有說出『專案系統分析更新』，AI 絕對不用動、嚴禁改動 `docs/system-design/`**！
3. **README 凍結防線**：**日常開發任務中，AI 絕對禁止隨意改動 `README.md`**；`README.md` **只有在使用者要 Push 時才進行建立與更新**！
4. **Push 前置主動提問攔截**：當使用者下達 `push到dev` 或 `push到master` 時，AI 在推送前**強制主動向使用者詢問**：
   > 「檢測到您即將執行遠端發布推送。請問本次推送是否需要同步建立/更新系統分析文檔 (`docs/system-design/`) 與 `README.md`？[Y/N]」
   - 使用者回覆「Y」➔ 啟動本章 SOP 完整更新。
   - 使用者回覆「N」➔ 完全略過，以極速直接推送！

---

### 一條龍執行 SOP (通用於前端、後端、全端與各領域專案)

本執行程序規範系統分析建立或更新之一條龍標準作業步驟：

1. **全專案原始碼深度逆向稽核 (Full Codebase Reverse Engineering)**:
   - 深度掃描實際檔案目錄、入口組件、路由分流機制、狀態 Context / Store、資料庫 / 本地快取 Schema、以及外部雲端 BaaS / API 介面。
   - 逐一比對 `docs/system-design/` 現存全部子文檔（依專案類型之 01~N 連續閉環全集）與 `README.md`，清查所有不吻合真實代碼之虛構描述、殘留過期規格、版本錯置（如依賴庫版本）與未實作空殼檔案。
2. **系統設計文檔庫精準建立/校準 (SSOT System Alignment)**:
   - 嚴格落實第 5.3 節「文檔動態裁剪原則」，按專案類型建立或更新對應必備文檔，豁免文檔嚴禁建立空殼。
   - 將實際代碼變更反哺至 `docs/system-design/`，確保所有類別名稱、方法合約、狀態機節點與資料流向 100% 忠實反映真實原始碼。
3. **根目錄 README.md 結構與圖表同步 (Root README Alignment)**:
   - **完全遵循 `AGENTS-GIT.md` 第 4 章法定權威規格**：將 C4 模型、UML 類別圖與雙向循序圖同步至 `README.md`，落實六大法定章節結構與中英雙語自然緊鄰排版。
4. **全圖表視覺風格與尺寸量體統一 (Diagram Harmonization)**:
   - 全面審視專案內所有 Mermaid 圖表（Flowchart、Class Diagram、Sequence Diagram、C4 Model），全面落實第 6 章與 `AGENTS-GIT.md` 第 4.3 節核心規格：
     - **風格絕對統一**：深邃黑夜背景、沉穩卡片、高科技邊框與光束連線一體化。
     - **尺寸量體均衡**：同一頁面圖表長寬比與節點量體等比對齊（約 750px × 360px 黃金比例），徹底消滅「忽大忽小、比例失衡」。
     - **消除繞圈巨線**：連線單向流動，筆直俐落，去除多餘繞道與巨大外框線。
5. **深淺雙模式高對比度與雙語標準審查 (Dual Mode & Bilingual Audit)**:
   - 檢驗所有 `subgraph` 容器，確認強制配置 `clusterBkg`、`clusterBorder` 與顯式 `style SubgraphId ...`，徹底杜絕刺眼白底穿透。
   - 審查深色與淺色模式下文字與線條清晰度，確保文字不發白、線條不發虛，符合 WCAG 2.2 AA 高對比度標準。
   - 檢查所有圖表節點、連線動作與章節標題，確認 100% 落實中英雙語緊鄰對照。
6. **語法硬指標與 Viewer 零崩潰驗證 (Zero-Crash Syntax Check)**:
   - 執行自動化檢驗腳本，全面掃描 Markdown 文件中所有 Mermaid 區塊：
     - 檢查連線標籤含有特殊字元者是否 100% 經雙引號包裹。
     - 檢查 `classDiagram` 之 stereotype 是否全數為標準單詞 ASCII 標籤。
   - 確保在 VS Code 預覽器、GitHub 網頁端及各大 Markdown Viewer 開箱即用、零 Syntax Error、秒開渲染。
7. **專案編譯與動態文檔結轉 (Build & Tracking Sync)**:
   - 執行專案構建指令（如 `pnpm run build`），確保 0 型別錯誤、生產打包正常通過。
   - 完全刷新 `docs/checklist.md`，並以純繁體中文將本次重構成果追加至 `docs/log.md` 最上方。
   - 移交 `AGENTS-GIT.md` 執行 Pre-Git-Add 一票否決審查，於本地 `feature` 沙盒分支完成原子存檔，嚴禁未授權推送到基準分支。