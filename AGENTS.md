# ⚡ AGENTS.md | 全域 AI Agent 最高工業級核心工程協定

> **專案作者**: 許哲誠 (HSU, CHE-CHENG)  
> **標準規範**: AI 與人類協作之最高工業級生產標準。本文件為所有 AI Agent 之一條龍全域核心中樞協定。負責系統優先級裁決、全域指令前置確認防禦、溝通互動約束、軟體工程底線 (SOLID / Clean Code)、頂級系統分析師文檔工程架構 (`docs/system-design/`) 以及特化模組協定調度。  
> *Release: 2026-09-14*

---

## ⚡ 核心交付指令速查 (Core Command Reference)

本區塊定義全域工程交付之總指揮指令。版控操作強制移交 `AGENTS-GIT.md`；特化協定（FRONT, BACK, UNITY）為純靜態規範庫，不包含任何指令。**注意：執行指令前強制觸發第 0.2 節人類確認機制**：

* 指令：`專案最佳化` —— 執行非 Unity 專案之一條龍代碼淨化、文檔同步、註解審查、依賴確認與暫存前置準備（**⚠️ 嚴格禁令：Unity 專案嚴禁執行此指令，避免破壞 GUID 與資產管線**，觸發第 7 章）。

---

## 🧭 Agent 階段生命週期與模組掛載地圖 (Lifecycle & Module Router)

為加速 Agent 閱讀並避免注意力稀釋，Agent 介入專案時請依據專案特徵動態掛載模組，並直接跳轉對應章節進行局部深讀：

```
                    [ 專案啟動 / 接收指令 ]
                              │
               【閱讀 AGENTS.md 第 0 章：優先級、路由與確認機制】
                              │
          ┌───────────────────┼───────────────────┐
          ▼                   ▼                   ▼
    [ Web/前端專案 ]    [ 伺服端/後端專案 ]    [ Unity 遊戲專案 ]
    掛載 AGENTS-FRONT    掛載 AGENTS-BACK    掛載 AGENTS-UNITY
    (靜態規範庫無指令)   (靜態規範庫無指令)   (靜態規範庫無指令)
          │                   │                   │
          │                   │            [ 嚴禁執行最佳化 ]
          │                   │                   │
          └───────────────────┼───────────────────┘
                              ▼
                 [ 涉及版控/提交/分支/推送 ]
                      掛載 AGENTS-GIT
                              │
                    [ 收到指令：專案最佳化 ]
               【強制先執行第 0.2 節二度確認 -> 執行第 7 章】
```

---

## 快速導航與規則交叉索引 (Rule Cross-Reference Index)

本索引表供所有執行任務之 AI Agent 於接收指令瞬間進行前置校準。

| 任務情境與檢查項目 | 強制參照章節與核心約束條款 |
| :--- | :--- |
| **協定載入、環境識別、模組路由** | 強制參照 **第 0.0 節**：專案啟動環境嗅探與特化協定掛載矩陣。 |
| **優先級仲裁順序與衝突處置** | 強制參照 **第 0.1 節**：落實停等提問協定與優先級裁決矩陣。 |
| **全指令執行前強制二次確認機制** | 強制參照 **第 0.2 節**：接收任何 AGENTS 指令前，必須向使用者確認始得執行。 |
| **語言文字、術語保留、註解雙軌制** | 強制參照 **第 1 章**：繁體中文（臺灣「臺」）、保留標準英文術語、頂部中文 Header 與內部純英文註解。 |
| **Git 提交、版控工作流、認證與指令** | 強制參照 **第 2 章與 AGENTS-GIT.md**：強制主動讀取根目錄之 `AGENTS-GIT.md` 執行遠端綁定（SSH 優先/容許 HTTPS）、重置化、三層分支策略、原子回滾與發布。 |
| **軟體品質、SOLID 實作、高內聚低耦合** | 強制參照 **第 3 章**：五大原則落地細則、Clean Code 函數約束與防禦性設計。 |
| **通用檔名與特化覆寫原則** | 強制參照 **第 4 章**：通用預設 kebab-case，特化協定具備最高覆寫權。 |
| **文檔追蹤與單次點收機制** | 強制參照 **第 5.1 與 5.2 節**：check-list 每次重寫、change-log 純中文單向追加。 |
| **系統分析師規範庫 (docs/system-design/)** | 強制參照 **第 5.3 與第 6 章**：Mermaid 類別圖、資料流、流程圖全集規範 (VS Code 即時渲染)。 |
| **專案交付最佳化一條龍** | 強制參照 **第 7 章**：指令「專案最佳化」總指揮 SOP（Unity 專案強制禁止使用）。 |

---

## 0. 🏆 協定架構、模組路由、優先級裁決與確認機制

本章節確立專案啟動時之協定掛載原則、全域指令安全確認防線，以及規則發生歧異時之裁決順序。

### 0.0 專案啟動環境嗅探與模組路由協定 (Modular Router Protocol)
本條款規範 Agent 介入任何專案時之自動化環境偵測流程：
1. **核心母體**: `AGENTS.md` 永遠常駐生效。
2. **版控防線**: 若專案包含 `.git` 或涉及版控操作，強制載入 `AGENTS-GIT.md`，所有 Git 行為 100% 移交該協定。
3. **特化領域靜態規範庫 (純規範、無指令)**:
   - 檢測到 `Assets/`、`ProjectSettings/`、`.unity` 檔：強制掛載 `AGENTS-UNITY.md`。
   - 檢測到 `package.json`、`vite.config.*`、前端框架目錄：強制掛載 `AGENTS-FRONT.md`。
   - 檢測到 `pom.xml`、`build.gradle`、後端 API 路由或資料庫目錄：強制掛載 `AGENTS-BACK.md`。
4. **全量丟入容錯**: 若使用者將全部協定放入專案根目錄，Agent 維持單一職責原則，僅在處理特定領域時調用對應特化協定，嚴禁跨領域混用規則。

### 0.1 優先級仲裁順位表
本條款定義不可逾越的規則裁決階層：
1. **歧義與未明確需求強制提問**: 當使用者指令缺乏核心參數、技術選型依據不明確或方向模糊時，Agent 嚴禁擅自主張或代為腦補，必須立即暫停並主動向人類提出具體問題釐清方向。
2. **需求衝突自動攔截**: 若新提示詞與 `docs/system-design/` 現存架構規格或核心資安規則衝突，Agent 必須立即暫停執行，主動向人類回報衝突細節，等待指示後始得繼續。
3. **資安、智財合規與零幻覺原則**: 零信任架構、金鑰隔離、開源授權嚴格審查；若所需之套件、API 或資材於合法公開管道查無實據，嚴禁捏造，必須主動回報。
4. **特化協定原生條款 (Domain Specific Overrides)**: 特化協定（`AGENTS-UNITY.md`, `AGENTS-FRONT.md`, `AGENTS-BACK.md`）中的語言原生語法與檔名規範，強制覆蓋通用規範。
5. **嚴格修訂邊界 (Scope Isolation)**: 當指令下達明確修改範圍時（例如「修改按鈕顏色」），絕對禁止改動、重構或變更該範圍以外的任何代碼、文字或樣式。
6. **人類明確覆寫**: 僅當使用者明確指示「忽略某規則」時，才允許暫時覆寫，並須於最終任務回報中註明。
7. **全域 Agent 協定**（本文件規範）。

### 0.2 全域指令前置確認原則 (Command Pre-Execution Confirmation Protocol)
本條款確立所有 Agent 法定指令執行前之絕對防禦防線，杜絕未經授權之自動化執行：
* **強制確認邊界**: 凡接收到 `AGENTS.md` 所定義之指令（如 `專案最佳化`）或 `AGENTS-GIT.md` 所定義之指令（如 `初始化 git`、`git 重置化`、`倒回上一個 commit`、`切回 dev`、`切回 master`、`pull 最新`、`push到dev`、`push到master`）時，**AI 絕對嚴禁未經確認直接執行任何實質變更**！
* **停等提問格式**: AI 必須立即中斷流程，並使用純繁體中文向使用者提出二次確認：
  > 「檢測到您下達法定指令：『**[指令名稱]**』。
  > 該操作即將執行：[簡述本指令之核心動作與影響範圍]。
  > 請問是否確認立即執行本指令？[Y/N]」
* **確認後放行**: 僅當使用者明確回覆同意（如「Y」、「確認」、「執行」）後，AI 始得進入該指令之標準作業程序（SOP）；若使用者回覆拒絕（如「N」、「取消」），則立即終止該指令並回報待命。

---

## 1. 🧠 互動協定、語言矩陣與排版要求

本章節嚴格規範 Agent 與開發者溝通時之文字風格、排他原則、標題排版及雙軌註解標準。

### 1.1 語言矩陣與專業術語規範
本條款定義對話與代碼之語言表達標準：
* **對話語言**: 於對話介面中，Agent 必須永遠使用**繁體中文 (Traditional Chinese)** 進行回覆。臺灣的「臺」一律使用「臺」，嚴禁使用簡體字、簡寫或「台」。
* **英文術語保留**: 保留業界標準英文術語（例如 Async / Await, Shader, Middleware, Controller, DTO, Repository, Dependency Injection, ScriptableObject, Redux, Hook, Mermaid）。
* **句式禁令**: 嚴禁中英夾雜句式，嚴禁中文後括號補註英文。
* **程式碼內部雙軌註解規範**:
  * 每個腳本/程式檔**頂部必須具備繁體中文 Header 區塊註解**，詳盡說明該腳本之核心功能、架構職責、依賴關係與生命週期（參照第 3.3 節）。
  * 頂部說明之後的其餘代碼內部註解，強制使用**頂級專業英文**，專注解釋架構意圖（"Why"）而非陳述字面操作（"What"）。

### 1.2 排版與排版格式規範
本條款規範文字產出之結構排版要求：
* **導言強制規範**: 每個標題（`#`, `##`, `###`）下方**必須具備一段導言說明**，嚴禁在標題後直接放置小標題、清單或程式碼區塊。
* **回報精準度**: 任務完成後，精準摘要變更檔案清單、關鍵架構決策與驗證狀態。

---

## 2. 🛡️ 外部協定鏈結與獨立性原則

本章節確立專案模組化協定之調用邊界與獨立運作能力。

* **AGENTS-GIT.md 唯一授權**: 所有涉及 Git 初始化、遠端認證（SSH 優先/HTTPS 容許覆寫）、原子暫存與提交、三層分支拓撲、版本維護（倒回 commit / 切回 dev / 切回 master / pull 最新）以及遠端發布推送（push到dev / push到master），本文件不作重複定義，Agent 必須主動查閱並 100% 遵循根目錄之 `AGENTS-GIT.md`。
* **特化規範庫單向查詢**: `AGENTS-FRONT.md`、`AGENTS-BACK.md` 與 `AGENTS-UNITY.md` 僅作為靜態標準規範庫，Agent 依專案類型主動讀取，不得期待其提供額外命令。
* **獨立運作保證**: 本文件不依賴特定特化協定即可常駐運作；若缺乏特化協定，Agent 則遵循一般 Clean Code 與防禦性原則。

---

## 3. 💎 極致工程標準：Clean Code、SOLID 與高內聚低耦合

本章節定義所有程式碼產出之絕對底線，不論任何語言或框架，皆必須貫徹頂級軟體工程原則。

### 3.1 SOLID 原則強制實踐
本條款細化物件導向與模組架構之五大核心原則：
* **單一職責原則 (SRP)**: 一個 Class 或模組僅能因單一理由變更。禁止出現萬能類別（God Object）。資料傳輸（DTO）、資料存取（Repository）、商務決策（Service）與協定轉換（Controller）必須物理隔離。
* **開放封閉原則 (OCP)**: 對擴充開放，對修改封閉。新增商業邏輯時，應透過多型（Polymorphism）、策略模式（Strategy Pattern）或組合（Composition）達成，嚴禁無止境修改既有核心 switch-case 或 if-else 分支。
* **里氏替換原則 (LSP)**: 子類別必須能夠完全替換其父類別而不破壞程式行為與合約。嚴禁子類別拋出 `NotSupportedException` 或覆寫成空函式。
* **介面隔離原則 (ISP)**: 介面應精簡明確，客戶端不應被迫依賴其未使用的方法。強制將龐大胖介面拆分為細粒度角色介面（Role Interfaces）。
* **依賴反轉原則 (DIP)**: 高階模組不應依賴低階模組，兩者皆應依賴抽象；抽象不應依賴細節，細節應依賴抽象。全面導入依賴注入（Dependency Injection, DI）或控制反轉（IoC）。

### 3.2 Clean Code 與防禦性程式設計
本條款規範函數實作與錯誤防護之工程要求：
* **小而精煉的函數**: 單一函數長度原則上不超過 20 行，且僅完成一件事。抽象層級必須單一。
* **早期返回 (Early Return / Guard Clauses)**: 將所有無效參數、邊界失敗或例外狀況於函數前端阻斷，減少巢狀層級。
* **嚴禁靜默吞掉例外 (Swallowed Exceptions)**: 捕獲例外必須記錄結構化 Context 或向上拋出領域專屬例外（Domain Exception）。

### 3.3 腳本頂部繁體中文註解強制規範 (Script Header Documentation)
本條款要求所有程式檔案頂部必須強制包含繁體中文 Header 區塊註解：
```csharp
/**
 * ============================================================================
 * 檔案名稱: OrderPaymentService.cs (或 .ts / .java)
 * 所屬模組: Order System (訂單核心模組)
 * 責任描述: 負責處理交易授權、冪等性防重送校驗與金流狀態更新。
 * 設計模式: 套用 Strategy 模式切換多元金流渠道。
 * 依賴關係: 依賴 IPaymentGateway 與 IOrderRepository。
 * 邊界處理: 處理網路超時重試、重複扣款防禦與併發鎖定。
 * ============================================================================
 */
```

---

## 4. 🔠 檔案系統命名原則與覆寫層級

本章節規範全域檔案系統之通用基底命名風格與各領域覆寫權限。

* **通用預設風格**: 專案一般配置檔、文檔目錄、工具腳本預設強制採用連字號 **`kebab-case`**（例如 `system-config.json`, `setup-env.sh`）。
* **特化覆寫條款**:
  * Unity 專案之 C# 腳本強制遵循 `AGENTS-UNITY.md` 採用 **`PascalCase.cs`**。
  * 後端 Java / C# 類別檔案強制遵循 `AGENTS-BACK.md` 採用 **`PascalCase.java`**。
  * 前端 UI 元件檔案強制遵循 `AGENTS-FRONT.md` 採用 **`PascalCase.tsx`**。

---

## 5. 📜 專案文檔工程矩陣與系統分析師規範 (`docs/`)

本章節確立專案唯一真實來源（Single Source of Truth），規範頂級系統分析師（SA）與軟體架構師之視覺化規格標準，並建立動態追蹤與按需裁剪機制。

### 5.1 專案目錄樹與文檔拓撲結構
本結構圖定義標準工程文檔之目錄佈局：
```
docs/
├── check-list.md              # 單次對話點收清單 (每次對話完全覆寫刷新)
├── change-log.md              # 全中文修訂歷程 (純中文單向持續追加歷程)
└── system-design/             # 系統架構設計專案文檔庫 (專案唯一規格真實來源，按需選配)
    ├── 01-overview.md         # 系統目標、邊界、業務價值與 C4 Model 架構拓撲圖
    ├── 02-tech-stack.md       # 技術選型矩陣、框架與相依套件庫規劃 (Web 強制標註 pnpm)
    ├── 03-project-structure.md# 儲存庫實體結構、分層職責與模組依賴關係拓撲圖
    ├── 04-functional-specs.md # 模組規格、RBAC 權限、狀態轉移機與極限邊界防禦
    ├── 05-flowcharts.md       # 核心業務流程圖、使用者操作流與異常容錯流程 (Mermaid)
    ├── 06-database-design.md  # 實體關聯圖 (ERD)、資料字典、資料交互流與快取防禦
    ├── 07-uml-diagrams.md     # 系統分析師規格：Mermaid 類別圖 (Class) 與循序圖 (Sequence)
    ├── 08-api-specifications.md# RESTful / WebSocket 通訊協定與 RFC 7807 錯誤格式
    ├── 09-security-auth.md    # 雙 Token 機制、金鑰環境變數隔離與資安防護
    ├── 10-ui-ux-standards.md  # 跨裝置 RWD、雙主題 WCAG 對比度與視覺呈現規範
    └── 11-devops-deployment.md# Docker 多階段構建、CI/CD 與可觀測性結構化日誌
```

### 5.2 動態追蹤文檔維護矩陣
本矩陣明訂每次對話與任務執行時動態文檔之更新觸發標準：
| 文檔名稱 | 觸發更新條件 | 強制執行動作與格式 | Git 版控狀態 |
| :--- | :--- | :--- | :--- |
| **`docs/system-design/`** | 專案初始化、新增功能、調整架構或更動 API/DB/流程時 | **專案單一真實來源 (SSOT)**：所有功能清單、架構設計、Mermaid 類別圖、流程圖與資料模型直接於所屬子文檔更新補齊。 | **正常納入 Git**<br>(依 AGENTS-GIT.md 交付) |
| **`docs/check-list.md`** | 每次使用者提出要求並執行任務時 | **單次對話完全覆寫刷新**：每次對話重新生成，記錄本次使用者明確要求與 AI 實際執行動作。多人協作支援請參照 `AGENTS-UNITY.md` 衝突預防機制。 | **正常納入 Git**<br>(依 AGENTS-GIT.md 交付) |
| **`docs/change-log.md`** | 每次任務完成或準備提交暫存前 | **純中文單向持續追加**：嚴禁修改舊紀錄。每次 `check-list.md` 完成之內容，直接同步搬移至最下方。格式：`[YYYY-MM-DD HH:mm] \| [類型] \| [使用者要求與實際執行細節] \| [影響模組]`。 | **正常納入 Git**<br>(依 AGENTS-GIT.md 交付) |

### 5.3 文檔動態裁剪原則 (System Design Pruning Protocol)
本條款確立防止文檔過度工程之裁剪底線，確保所有系統皆具備系統分析師規格之視覺化圖表：
| 專案類型與技術範疇 | 強制必備核心文檔 (Mandatory) | 自動豁免/嚴禁建立 (Exempted) |
| :--- | :--- | :--- |
| **完整全端系統** | `01` 至 `11` 全套必備 | 無 |
| **現代 Web / 純前端專案** | `01`, `02`, `03`, `04`, `05`, `07`, `10` | `06` (實體 DB), `08` (實體後端 API 實作), `09` (後端驗證), `11` (後端部署) |
| **純單機遊戲 (Unity)** | `01`, `02`, `03`, `04`, `05`, `07` | `06` (DB/快取), `08` (API), `09` (Token), `11` (Docker) |
| **純後端 / 微服務** | `01`, `02`, `03`, `04`, `05`, `06`, `07`, `08`, `09`, `11` | `10` (UI/UX 規範) |
| **輕量工具庫 / CLI** | `01`, `02`, `03`, `07` | `04`, `05`, `06`, `08`, `09`, `10`, `11` |

> **豁免執行禁令**: 若某項目被判定為「豁免/不建立」，在目錄內**絕對禁止建立填寫「無」或留白的空殼檔案**。

---

## 6. 🛠️ 系統分析師與架構設計全集規格 (`docs/system-design/` 核心細節)

本章節為系統架構設計之全集技術規範標準。文件撰寫必須達到**專業系統分析師 (SA)** 與**頂級架構師**之標準，採用現代通用之 **Mermaid** 語法，確保在 VS Code 與 GitHub 中開箱即用、免額外套件即可原生即時圖形渲染。

### 6.1 核心視覺化規格要求 (Mermaid Diagram Standards)
本條款規範系統設計文件必須具備的三大核心 Mermaid 圖表規格：
1. **Mermaid 類別關係圖 (`07-uml-diagrams.md`)**:
   - 強制採用標準 Mermaid `classDiagram` 語法，確保在 VS Code 與 GitHub 免額外外掛直接原生即時渲染。
   - 清楚標註類別與介面名稱、成員可見性（`+` Public, `-` Private, `#` Protected）、核心方法與參數型別。
   - 明確繪製 Class 間的四大核心關係：繼承（`<|--`）、實作（`<|..`）、組合（`*--`）、聚合（`o--`）與依賴（`..>`）。
2. **模組與檔案依賴拓撲圖 (`03-project-structure.md`)**:
   - 使用 Mermaid `flowchart` 展示系統檔案間之引用階層與呼叫流向，呈現高內聚低耦合之架構防線。
3. **跨層資料交互循序圖 (`05-flowcharts.md` / `07-uml-diagrams.md`)**:
   - 使用 Mermaid `sequenceDiagram` 完整展示使用者觸發事件、前端 Component、狀態 Context、自研 CMS 與遠端 API / BaaS 之間的資料同步、狀態轉移與錯誤回滾流程。

---

## 7. 🚀 協定：專案最佳化 (Project Optimization)

本章節規範收到執行指令：「**專案最佳化**」時，Agent 必須強制執行之一條龍完整標準作業程序（SOP）。**注意：執行前必須依第 0.2 節向使用者提出二次確認**。

### ⚠️ Unity 專案絕對禁令 (Strict Ban for Unity)
本條款建立 Unity 專案資產管線保護紅線：
* **檢測到 Unity 專案特徵時（存在 `Assets/`、`ProjectSettings/` 等），Agent 嚴禁執行本章節之最佳化指令！**
* **攔截原因**：Unity 資產依賴 GUID 與 `.meta` 檔案綁定，全端檔案淨化與結構調整極易破壞 Unity 資產引用關係。
* **回報模板**：「⚠️ 警告：當前為 Unity 遊戲專案，本協定嚴格禁止對 Unity 執行『專案最佳化』指令，以防破壞資產管線與 GUID 參照。請依據 `AGENTS-UNITY.md` 規範進行手動審查與維護。」

---

### Web / 全端專案一條龍執行 SOP (僅限非 Unity 專案)

本執行程序規範 Web 與伺服端專案之一條龍標準交付步驟：

1. **套件管理器校驗與切換提問 (Package Manager & pnpm Audit)**:
   - 檢查當前專案是否由 npm 管理（存在 `package-lock.json`）。
   - 若是，**必須立即暫停並主動向使用者提問**：
     > 「檢測到當前專案仍在使用 npm 管理（存在 package-lock.json）。『專案最佳化』標準要求全面升級為 pnpm。請問是否同意切換至 pnpm 並清除舊有 npm 內容？[Y/N]」
   - 若使用者同意（Y），立即執行徹底清除 SOP：刪除 `package-lock.json` 與 `node_modules/`，執行 `pnpm install` 生成 `pnpm-lock.yaml`；若使用者拒絕（N），則維持現狀並於回報中載明。
2. **系統設計文檔檢視**: 讀取並檢視 `docs/system-design/` 相關模組，確保本次改動與系統規格完全吻合。
3. **單次點收與歷程結轉**: 完全刷新 `docs/check-list.md`，並以純中文將本次產出向下追加至 `docs/change-log.md`。
4. **代碼品質與命名審查**: 確保所有新寫或修改之腳本頂部具備完整的**繁體中文 Header 區塊註解**；私有成員落實 **`private _camelCase`**；若有欠缺後端或其他依賴，落實繁體中文 `TODO:` 規格；審查 Clean Code、SOLID 原則與早期返回。
5. **代碼庫淨化**: 移除無用代碼（Dead Code）、未使用的套件依賴、殘留除錯日誌與空白目錄。
6. **資材合理性檢驗**: 檢查資產檔案引用路徑是否正確，無破損無效連結。
7. **UI/UX 相容性審查**: 審查全裝置直橫向 RWD 相容性與深淺色模式 WCAG 2.2 AA 對比度（若屬免除 UI 之專案類型則跳過）。
8. **前端體驗與 SEO 規範審查**: 涉及 Web/前端專案時，強制委託 `AGENTS-FRONT.md` 執行語意化 HTML、結構化資料標記（JSON-LD）、Meta 標籤矩陣與無障礙相容檢驗。
9. **環境變數與暫存前置準備**: 同步 `.env.example`，確認動態文檔就緒，並移交 `AGENTS-GIT.md` 執行雙語 `README.md` 結構驗證。