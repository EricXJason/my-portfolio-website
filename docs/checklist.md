# 本次任務清單 (Session Check-List)

本文件依據《AGENTS.md》全域最高工業級工程協定規範，進行本次對話「正式建立 AI 極致效能防衛架構：變更日誌更名為 log.md（Push 永久保留、日常上限 50 筆）、Git 分支全量標色、一般文字色彩重點強化、新增指令『專案系統分析建立』與更名『專案系統分析更新』、確立日常文檔與 README 凍結及 Push 前置提問機制」之單次點收全量重寫刷新，詳實記錄使用者提出之指示與具體執行細節。

---

## 使用者明確指示

1. **變更日誌更名與滾動保留機制**：
   - 將變更日誌正式更名為 `log.md`（簡潔有力）。
   - **Push 釋出紀錄強制 100% 永久保留，絕不刪除**。
   - 其餘日常本地 Commit 紀錄最多保留最新 **50 筆**，超出自動滾動淘汰，解決大檔案拖慢 AI 解析速度之問題。
2. **Git 分支（master / dev / feature）全面色彩標註**：
   - 不只 Commit 標色，分支名稱強制全面標色：
     - `master`: 珊瑚紅 `<font color="#f87171"><b>master</b></font>`（生產主線）
     - `dev` / `development`: 琥珀金 `<font color="#fbbf24"><b>dev</b></font>`（整合測試）
     - `feature`: 天青藍 `<font color="#38bdf8"><b>feature</b></font>`（開發沙盒）
3. **一般文字敘述加強色彩重點排版（非花俏，重在秒速抓重點）**：
   - 杜絕大段單調黑白灰純文字，對核心架構名詞、關鍵量化指標、安全禁令進行視覺色彩強化，提升可讀性與掃描效率。
4. **Agent 處理速度極致優化與指令重構**：
   - 新增指令：**`專案系統分析建立`**（第一次專案啟動時執行，生成 `docs/system-design/`）。
   - 指令改名：**`專案系統分析重構` ➔ `專案系統分析更新`**（重大架構變更時手動觸發更新）。
   - **核心凍結防線**：
     - 使用者尚未說出 `專案系統分析建立` 之前，**嚴禁生成 `system-design`**。
     - 使用者沒有說出 `專案系統分析更新`，日常微小修改、Bug 修復**絕對不用動、嚴禁改動 `system-design`**。
     - **`README.md` 日常開發凍結**，只有在要 Push 時才進行建立和更新，日常任務不隨意亂動。
     - **Push 前置主動提問攔截**：當使用者要 Push（`push到dev` 或 `push到master`）時，AI 強制主動詢問是否同步建立/更新系統分析文檔與 README。

---

## AI 實際執行細節

1. **Git 檔案重命名與日誌裁剪**：
   - 執行 `git mv docs/changelog.md docs/log.md`。
   - 實作滾動保留演算法：Push 相關節點（8 筆）永久保留，其餘日常紀錄精準收斂至最新 50 筆，總計 59 筆高信噪比紀錄。
   - 全面為紀錄內的分支標籤（`master`、`development`、`feature`）注入專屬語意色彩。
2. **母協定《AGENTS.md》全面法典化修訂**：
   - **指令速查（第 9-16 行）**：確立 `專案系統分析建立` 與 `專案系統分析更新` 兩大指令，並加掛「AI 極致效能與日常零干擾底線 (Zero-Overhead Performance Firewall)」重點 Alert。
   - **一般文字敘述色彩標準（第 1.4 節）**：增訂第 2 條「一般文字敘述之重點色彩標排標準」，確立核心架構名詞反引號、量化指標反白、安全限制珊瑚紅突出規範。
   - **分支標色標準（第 1.4 節）**：正式將 `master` (紅)、`dev` (金)、`feature` (藍) 三大分支標準色納入法典。
   - **目錄結構拓撲與維護矩陣（第 5.1 & 5.2 節）**：全面更新為 `docs/log.md`，並明文寫入日常凍結防線與 Push 永久保留 + 50 筆上限機制。
   - **第 8 章全面重構**：正式更名為《8. 專案系統分析建立與更新》，明訂四大邊界防禦與 Push 前置提問攔截協定。
3. **版控協定《AGENTS-GIT.md》同步升級**：
   - 第 4.1 節確立 `README.md` 日常開發任務凍結防線，杜絕微小提交頻繁改動 README。
   - 第 5 章於 `push到dev` 與 `push到master` 流程前端植入強制提問防禦。
4. **專案構建驗證與純英文 Git 提交**：
   - 執行 `pnpm run build` 確認 0 錯誤（301ms 構建通過）。
   - 遵循 100% 英文 Conventional Commits 規範完成提交。
